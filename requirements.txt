# Core Dependencies
pygame>=2.5.0
pytest>=7.0.0

# Optional Raspberry Pi Hardware Dependencies
# (Installed on Raspberry Pi OS for physical button, LCD, and buzzer support)
gpiozero; sys_platform == 'linux'
RPLCD; sys_platform == 'linux'
smbus2; sys_platform == 'linux'
